# GitHub action workflow for Asterion
name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [opened, review_requested]
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "test"
  test:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      # Setup python
      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8 # TODO: more python versions
      # Install the package with tests requirements
      - name: Install package
        run: |
          python -m pip install --upgrade pip
          pip install -e .[tests]
      # Runs pytest
      - name: Run tests
        run: pytest .
      # # Stage and commit any new files if files have been staged
      # - name: Commit files
      #   run: |
      #     git config --local user.email "action@github.com"
      #     git config --local user.name "GitHub Action"
      #     git add -A
      #     if [ "$(git diff --staged)" ]; then git commit -m "Do action" -a; fi
      # - name: Push changes
      #   uses: ad-m/github-push-action@v0.6.0
      #   with:
      #     # Token for the repo. Can be passed in using $\{{ secrets.GITHUB_TOKEN }}
      #     github_token: $\{{ secrets.GITHUB_TOKEN }}
      #     branch: main
